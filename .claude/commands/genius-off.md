---
description: Deactivate the genius UserPromptSubmit hook (removes the flag file)
allowed-tools: Bash(rm *)
---

Run the following command and report the result in one line:

```bash
rm -f ~/.agentic-workspace/genius-hook-on && echo "genius hook: OFF"
```
