---
description: Activate the genius UserPromptSubmit hook (writes the flag file)
allowed-tools: Bash(touch *)
---

Run the following command and report the result in one line:

```bash
mkdir -p ~/.agentic-workspace && touch ~/.agentic-workspace/genius-hook-on && echo "genius hook: ON"
```
